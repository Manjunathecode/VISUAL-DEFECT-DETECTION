# VISUAL-DEFECT-DETECTION
A deep learning-based computer vision application that detects defects in industrial casting products using a ResNet18 CNN model. The system is trained on real-world image data and deployed as an interactive web app using Gradio for real-time predictions with confidence scores.
